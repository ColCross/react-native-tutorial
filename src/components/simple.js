import React, { useState } from 'react';
import { ActivityIndicator, Button, Picker, Platform, StyleSheet, Text, View } from 'react-native';


const SimpleComponents = ({ ...props }) => {
    return (
        <View>
            <Text>Some simple components will go here!</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});


export default SimpleComponents;